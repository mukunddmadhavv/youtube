# Richard artwork

Place your original transparent PNGs here:

- `char_intro.png`
- `char_point_left.png`
- `char_point_right.png`

These are the names used by the reference agent. Change `character_assets` in
`pipeline.json` if your filenames differ, and describe each pose here.
Preserve the originals. Production-specific crops belong in the episode folder.
